[project]
name = "bitarray"
version = "0.0.0"
description = "native Python implementation providing an array of bit values represented as either 0 or 1"
readme = "README.md"
requires-python = ">= 3.12"
dependencies = []
